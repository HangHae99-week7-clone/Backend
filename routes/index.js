const express = require("express");
const Posts = require("./posts");
const Comments = require("./reviews");
const Users = require("./users");

const router = express.Router();

module.exports = router;
